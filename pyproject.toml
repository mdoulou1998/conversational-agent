[project]
name = "support-agent"
version = "0.0.0"
description = "A conversational support agent for a SaaS product"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "google-genai>=1.40.0",
    "isort>=9.0.1",
    "pydantic>=2.11.7",
    "ruff>=0.16.7",
]
